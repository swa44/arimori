# 아리모리 로컬 개발 안내

## 같은 와이파이의 모바일 기기에서 접속

개발 서버를 모든 네트워크 인터페이스에 열어 실행한다.

```bash
npm run dev -- --hostname 0.0.0.0
```

터미널의 `Network` 항목에 표시된 주소를 모바일 브라우저에서 연다.

```text
http://192.168.x.x:3000
```

`next.config.ts`의 `allowedDevOrigins`에는 다음과 같이 `192.168.x.x` 사설망 전체가 허용되어 있다.

```ts
allowedDevOrigins: ["192.168.*.*"]
```

따라서 장소나 공유기가 바뀌어 컴퓨터의 마지막 두 IP 숫자가 달라져도 설정을 다시 수정할 필요가 없다. 설정 파일을 변경한 경우에는 실행 중인 개발 서버를 종료한 뒤 다시 시작해야 한다.

이 설정은 Next.js 개발 서버에만 적용되며 Vercel 배포 환경에는 영향을 주지 않는다. 공용 와이파이에서는 같은 네트워크의 다른 기기가 개발 서버에 접근할 수 있으므로 테스트가 끝나면 개발 서버를 종료한다.
